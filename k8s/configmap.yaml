apiVersion: v1
kind: ConfigMap
metadata:
  name: dropoff-api-config
  namespace: user-dropoff
data:
  APP_ENV: "production"
  FLASK_ENV: "production"
  LOG_LEVEL: "INFO"
  GUNICORN_WORKERS: "4"
  GUNICORN_THREADS: "2"
  GUNICORN_TIMEOUT: "60"
